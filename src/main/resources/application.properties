server.port=9000

#con esta orden logramos que los datos no desaparezcan
#cuando reiniciamos la aplicaci�n
spring.datasource.url=jdbc:h2:~/peopledb-web

#ddl: the sql commands that create tables and structures
spring.jpa.hibernate.ddl-auto=update
spring.jpa.generate-ddl=true
spring.jpa.show-sql=true

#we don't mind what the size is (-1)
spring.servlet.multipart.max-file-size=-1
